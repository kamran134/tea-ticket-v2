# План: Сетка = реальные зоны/места + зона без мест

## Концепция
«Сетка» (`GridMapEditor`) перестаёт быть черновиком-калькулятором поверх `Venue.gridLayout`
и становится редактором над настоящими `Zone`/`Seat`. Закрашенная ячейка — либо реальное
место (`Seat`, для `type: SEATED`), либо просто часть визуальной области зоны без мест
(`type: GENERAL`, вместимость — отдельное число). Сетка используется и на покупке билета
вместо «Схемы». «Схема» (`ZoneMapEditor`/`VenueMap`) скрывается из UI, код не удаляется.

---

## Этап 1: Схема БД
- [x] Добавить `color String?` в модель `Zone` (для покраски на сетке)
- [x] Миграция `add_zone_color`

## Этап 2: Backend
- [x] `zones.ts`: добавить `color` в `createZoneSchema`/`updateZoneSchema`
- [x] `zones.ts` DELETE `/:id`: перехватывать FK-конфликт (P2003) → 409 с понятным сообщением
- [x] `venues.ts` `PUT /:id/grid-layout`: новая схема `{ rows, cols, cells: string[][] }`
      (без вложенного `zones[]` — ячейка ссылается на реальный `Zone.id`)
- [x] Транзакционная синхронизация мест для `SEATED`-зон при сохранении сетки:
      diff закрашенных ячеек ↔ существующие `Seat` (row/posInSection = координаты ячейки,
      sectionIndex = 0), запрет удаления ячейки/места с активным билетом (409),
      пересчёт `capacity` зоны

## Этап 3: Frontend типы + API
- [x] `types/index.ts`: `color` в `Zone`, `GridLayout` без `zones[]`, убрать `GridZone`
- [x] `api.ts`: обновить `saveGridLayout` под новую схему, вернуть `{ venue, zones }`

## Этап 4: Admin UI — GridMapEditor
- [x] Загружать реальные зоны венью через `api.getZones` (не только из `gridLayout`)
- [x] Форма «+Зона»: номер карты, чекбокс «Без конкретных мест»
      (снят → `SEATED`, отмечен → `GENERAL` + поле «Вместимость»)
- [x] `addZone` → `api.createZone` (реальная зона сразу)
- [x] `removeZone` → `api.deleteZone` (+ обработка 409 «есть билеты»)
- [x] `save()` → новый `api.saveGridLayout`, синхронизация мест на бэке, toast при 409

## Этап 5: Покупка билета
- [x] Новый компонент `VenueGridMap.tsx` — сетка зала, клик по ячейке выбирает зону
- [x] `RegisterForm.tsx`: приоритет gridLayout → legacy VenueMap → карточки
- [ ] Ручная проверка: `SeatPicker` корректно показывает места, сгенерированные из сетки

## Этап 6: Скрыть «Схему»
- [x] `ManagePanel.tsx`: убрать вкладку «Схема» из списка табов (код не трогать)

## Этап 7: Проверка
- [x] Typecheck/build backend и frontend (`tsc --noEmit` + `vite build` — чисто)
- [x] Задеплоено на прод (push в master → GitHub Actions → docker compose up):
      миграция `add_zone_color` применилась автоматически при старте бэкенда,
      колонка `color` подтверждена в БД, tea-ticket.com отдаёт 200, /health — ok
- [ ] Сценарий в браузере: зона с местами через сетку → покупка → занятая ячейка видна
      (не проверено вживую — рекомендуется прогнать на проде вручную)
- [ ] Сценарий в браузере: зона без мест через сетку → покупка по количеству гостей
      (не проверено вживую — рекомендуется прогнать на проде вручную)

---

## Порядок зависимостей
Этап 1 → Этап 2 → Этап 3 → Этап 4 → Этап 5 → Этап 6 → Этап 7

---

# Часть 2: фикс синхронизации мест + убрать номер карты (заглушка оплаты)

## Баг
`PUT /:id/grid-layout` синхронизировал места для *всех* зон типа SEATED в мероприятии,
а не только для тех, что реально участвуют в сетке. Зона, созданная через старую
вкладку «Зоны» (генератор рядов/секций), никогда не красилась на сетке — при любом
сохранении сетки её места считались «удалёнными из сетки» и попадали под защиту от
удаления забронированного места → ошибка 409 на ровном месте.

## Этап 8: Backend
- [x] `venues.ts`: синхронизировать места только для зон, которые сейчас в `cells`
      ИЛИ были в *предыдущем* сохранённом `gridLayout` (а не для всех SEATED-зон венью)
- [x] `zones.ts`: `cardNumber` — необязательное поле в create/update схемах
- [x] `schema.prisma`: `Zone.cardNumber` — `@default("")`, миграция
- [x] `cron.ts`: отключить автоистечение BOOKED через 1 час — оно било в старый флоу
      «не оплатил за час», а без онлайн-оплаты подтверждение полностью ручное

## Этап 9: Frontend — убрать номер карты из форм зон
- [x] `api.ts`: `createZone` — `cardNumber` необязателен
- [x] `ManagePanel.tsx`: убрать поле «Номер карты» из формы создания/редактирования зоны
- [x] `GridMapEditor.tsx`: убрать поле «Номер карты» из формы «+Зона»

## Этап 10: Frontend — заглушка вместо шага оплаты
- [x] `TicketView.tsx`: блок для `status === 'BOOKED'` — вместо номера карты и загрузки
      чека показывать заглушку («оплата картой скоро», без countdown/receipt-upload)
- [x] `ManagePanel.tsx` (вкладка «Билеты»): кнопки Подтвердить/Отклонить — тоже для
      `BOOKED`, не только `PENDING`; фильтр «Новые» (BOOKED) добавлен и стал дефолтным

## Этап 11: Проверка
- [x] Typecheck/build backend и frontend (`tsc --noEmit` + `tsc` + `vite build` — чисто)
- [x] Дождаться подтверждения перед пушем — меняется реальный флоу приёма денег

---

# Часть 3: Афиша на главной + страница мероприятия по слагу

## Концепция
`/` — афиша (карточки с постером, только active + дата в будущем).
`/e/:slug` — страница конкретного мероприятия (текущий RegisterForm, но без
выпадающего списка мероприятий — venue всегда определяется слагом из URL).
Один Vite entry (`index.html`/`main.tsx`), выбор Afisha/RegisterForm по pathname.

## Этап 12: БД
- [x] `Venue.slug String @unique`, `Venue.posterImage String?`
- [x] Миграция: добавить колонки, бэкофилл `slug = id` для существующих строк,
      `NOT NULL` + уникальный индекс

## Этап 13: Backend
- [x] `services/slug.ts` — транслитерация RU→EN + `generateVenueSlug(name, date)`
- [x] `POST /api/venues` — принимает необязательный `slug` (иначе автогенерация),
      409 при коллизии (`P2002`)
- [x] `PATCH /api/venues/:id` — добавить `slug`, `posterImage`
- [x] `GET /api/venues/slug-available?slug=&excludeId=` — проверка занятости (admin)
- [x] `GET /api/venues/by-slug/:slug` — публичный резолвинг для страницы мероприятия
      (только active, иначе 404 — как уже ведёт себя текущий флоу)
- [x] `POST /api/venues/:id/upload-poster` — по аналогии с upload-floor-plan
- [x] `GET /api/venues?upcoming=true` — только active + дата в будущем, сортировка по дате

## Этап 14: Frontend — типы и API
- [x] `types/index.ts`: `Venue.slug`, `Venue.posterImage`
- [x] `api.ts`: `getVenues` с `upcoming`, `getVenueBySlug`, `checkSlugAvailable`, `uploadPoster`,
      `createVenue`/`updateVenue` — `slug`

## Этап 15: Frontend — Афиша + роутинг
- [x] `Afisha.tsx` — карточки (постер/заглушка, название, дата) → ссылка `/e/{slug}`
- [x] `main.tsx` — по pathname рендерит `Afisha` или `RegisterForm(slug)`
- [x] `RegisterForm.tsx` — убрать список мероприятий, резолвить venue по `slug`-пропу
- [x] `nginx.conf` (frontend-контейнер) — `location /e/ { try_files $uri /index.html; }`

## Этап 16: Frontend — админка
- [x] Форма создания мероприятия: поле «Слаг» с live-автогенерацией из
      названия+даты, редактируемое, проверка занятости (debounce)
- [x] На карточке мероприятия: изменить слаг, загрузить постер
- [x] «Скопировать ссылку» → `/e/{slug}` вместо `/?venue={id}`

## Этап 17: Проверка
- [x] Typecheck/build backend и frontend
- [x] Задеплоено и проверено на проде: миграция применилась, /e/:slug и
      /api/venues/by-slug работают, ?upcoming=true фильтрует корректно

---

# Часть 4: корзина, гости без имён, шаблоны сетки, реорганизация админки

## Решения (подтверждены пользователем)
- Мульти-зона = корзина: добавляешь места/количество из разных зон (в т.ч.
  смешивая «с местами» и «без мест»), один чекаут (имя/телефон) в конце.
  Один `groupId` на все билеты корзины независимо от зоны.
- Гости без имён: у корзины есть общее число билетов N (первый — сам
  покупатель). Либо вводишь имена остальных N-1, либо просто оставляешь
  как есть — тогда имена авто: «Гость 1», «Гость 2»... Отображение по
  месту нужды в формате «Имя + N».
- Шаблоны сетки: просто именованные шаблоны (без сущности «заведение»).
  Шаблон хранит форму сетки + описание зон (имя/цвет/тип/вместимость для
  GENERAL) — БЕЗ цены/номера карты, их admin проставляет при применении.
- «Зоны» скрываем, но столы используются → перенести управление столами
  и редактирование цены/названия зоны в «Схему» (бывшая «Сетка»).
- Валюта — жёстко манат везде, дропдаун убрать.
- Заодно: форма «Редактировать мероприятие» (имя/дата — валюту не показывать,
  она теперь всегда манат) на карточке в «Мероприятиях».

## Этап 25: Backend — модель GridTemplate
- [ ] `GridTemplate` в schema.prisma: id, name, rows, cols, cells (Json,
      значения — "slot-N"/empty/blocked), zones (Json: [{slotId, name, color,
      type, capacity}]), createdAt. Миграция.
- [ ] `GET/POST/DELETE /api/grid-templates`, `GET /api/grid-templates/:id`

## Этап 26: Backend — валюта жёстко манат
- [ ] `createVenueSchema`/`patchVenueSchema`: currency больше не принимается
      от клиента, всегда `₼` на сервере
- [ ] Миграция: `UPDATE "Venue" SET currency = '₼'` (сейчас все и так ₼, для полноты)

## Этап 27: Frontend — GridMapEditor: редактирование зоны, столы, шаблоны
- [ ] Зона-чип: кнопка редактирования (имя/цена/вместимость для GENERAL) через
      существующий `api.updateZone`
- [ ] Блок «Столы»: создать зону-стол (имя/цена) + генерация столов/стульев
      (существующие `generateTables`/`getTables`), сброс
- [ ] «Сохранить как шаблон» / «Загрузить шаблон» — используют новые эндпоинты;
      загрузка создаёт реальные зоны из шаблона (цена=0, потом правится)
      и подставляет ячейки в локальный (несохранённый) стейт редактора

## Этап 28: Frontend — реорганизация админки
- [ ] Убрать вкладку «Зоны» из навигации (код не трогать)
- [ ] Переименовать вкладку «Сетка» → «Схема»
- [ ] Убрать дропдаун валюты из создания мероприятия, всегда слать `₼`
- [ ] Кнопка «Редактировать» на карточке мероприятия — форма имя/дата

## Этап 29: Backend — мульти-зонная регистрация
- [ ] `POST /api/tickets/register` — новая форма запроса:
      `{ name, phone, venueId, items: CartItem[], guestNames?: string[] }`,
      `CartItem` = `{zoneId, seatIds}` | `{zoneId, tableId, quantity}` |
      `{zoneId, quantity}`. Транзакция: валидация доступности по каждому item,
      плоский список персон (первая — покупатель, остальные — guestNames[i]
      или «Гость N»), общий `groupId`, totalPrice — сумма по всем билетам

## Этап 30: Frontend — RegisterForm: корзина
- [ ] Локальный стейт корзины вместо одиночного `zoneId`
- [ ] Клик по месту/зоне на сетке — добавление в корзину (не замена выбора);
      для «без мест» — степпер количества
- [ ] Блок корзины: список позиций, удаление, промежуточный итог
- [ ] Чекаут: имя/телефон покупателя + переключатель «указать имена гостей» /
      «просто количество»
- [ ] Отправка — новая форма запроса на `/api/tickets/register`

## Этап 31: Frontend — TicketView под мульти-зонные группы
- [ ] Сумма — по факту `members.reduce(price)`, а не `price * count`
      (у билетов группы теперь могут быть разные зоны/цены)
- [ ] Заголовок группы — формат «Имя + N»
- [ ] В списке участников группы — зона/место каждого

## Этап 32: Проверка
- [x] Typecheck/build backend и frontend — чисто
- [x] Ревью найденного бага: `applyTemplate` слал `price: 0`, а бэк требует
      `price > 0` — поправлено на плейсхолдер `1`
- [ ] Ручной сценарий в браузере (не выполнено — нет БД в этом окружении)
- [ ] Подтвердить перед пушем — самое крупное изменение флоу покупки за всё время

## Известные упрощения (сознательно, из-за объёма задачи)
- Корзина полностью работает для сетки (`VenueGridMap`) и столов. Для старой
  «Схемы» (`VenueMap`, зоны с `layoutData`) — тоже пишет в общую корзину,
  но это уже депрекейтед-путь (создавать такие зоны больше негде в админке)
- «Имя + N» и сумма по фактическим ценам — только на странице билета
  покупателя (`TicketView`). В списке билетов админки — как было, построчно
- В списке участников группы показывается зона каждого, без номера
  места/стола (это отдельный API-вызов, не стал усложнять)

---

## Этап 38: Правки сетки покупки + обязательный email

- [x] Админка (`GridMapEditor`): кнопка «На весь экран» видна всегда,
      без порога `cols>15 || rows>15`
- [x] Покупка (`VenueGridMap`): убран компактный inline-превью и угловая
      кнопка разворота — компонент теперь рендерится только как fullscreen-
      оверлей; в `RegisterForm` вместо превью — кнопка «Выберите места»
      (переходит в «Изменить выбор мест × N», когда в корзине уже есть места
      из сетки), клик сразу открывает сетку на весь экран
- [x] GENERAL-зоны: в оверлее названия зоны на сетке теперь показывается
      остаток («осталось: N» / «мест нет»); если `available <= 0` —
      ячейка и карточка в легенде некликабельны, модалка количества не
      открывается
- [x] Телефон: маска ввода (только цифры и один `+`, группировка по 3),
      звёздочка обязательности, валидация по количеству цифр (≥7) перед
      отправкой
- [x] Email: новое обязательное поле в форме покупки, HTML5
      `type="email"` + доп. regex-валидация перед отправкой
- [x] БД: `Ticket.email String?` (nullable — у старых билетов останется
      пустым), миграция `20260802010000_ticket_email`
- [x] Backend: `registerSchema` требует `email: z.string().email()`,
      сохраняется в `ticketRows`
- [x] Frontend: `Ticket.email` в типах, `api.register()` принимает `email`,
      отображается в `ManagePanel` (список билетов) и `TicketView`
      (карточка билета)
- [x] Typecheck backend (`tsc --noEmit`) и frontend (`tsc --noEmit` +
      `vite build`) — чисто
- [ ] Ручная проверка в браузере — не выполнена (в этом окружении нет
      Docker/локальной БД, см. `context.md`)
- [ ] Подтвердить перед пушем

---

## Этап 39: Сцена на схеме + столы с расположением на сетке

### Сцена (декоративный элемент)
- [x] `GridCellState` — новое значение `'stage'`
- [x] `GridMapEditor`: инструмент «Сцена» в тулбаре (переиспользует общий
      toggle-механизм покраски, без спецкейсов), заливка без внутренних линий
      + оверлей-надпись «Сцена» (тот же `zoneBoundingBoxes`, просто с
      `Set(['stage'])`)
- [x] `VenueGridMap`: то же самое на стороне покупателя — ячейка не кликабельна,
      чисто декоративная
- [x] Backend `gridLayoutSchema`/sync в `venues.ts`: `'stage'` пропускается
      наравне с `'empty'`/`'blocked'`, не считается неизвестной зоной
- [x] Шаблоны сетки (`GridTemplate`): `'stage'`-ячейки сохраняются и
      восстанавливаются как есть (не завязаны на slotId зон)

### Столы — расположение на сетке (было: не доделано, только плоский список)
- [x] БД: `ZoneTable.row/col Int?` (nullable — старые «россыпью»
      сгенерированные столы остаются без координат и не трогаются),
      `Zone.tableChairs Int?`; миграция `20260802030000_table_grid_placement`
- [x] Backend `zones.ts`: `tableChairs` в схемах create/update зоны,
      обязателен для `type: TABLE` (`refine`)
- [x] Backend `venues.ts` `PUT /:id/grid-layout`: TABLE-зоны теперь красятся
      на сетке; sync по образцу SEATED (диф по `row/col`, блокировка удаления
      стола с активным билетом, счётчик `number` продолжает с максимума).
      **Важно**: sync трогает только столы с `row != null` — старые
      «россыпью» столы (из `generate-tables`) не участвуют в диффе и не
      удаляются, даже если админ покрасит ту же зону на сетке
- [x] `GridMapEditor`: убрана отдельная секция «Столы» и весь bulk-flow
      (`generate-tables` UI, count/chairCount форма) — столы теперь обычная
      зона в общей палитре (SEATED/GENERAL/TABLE через `<select>` в форме
      «+Зона»), красятся точно так же, с полем «Мест за столом» вместо
      «Вместимость»; revenue/легенда учитывают `paintedCells × tableChairs`
- [x] `VenueGridMap`: подгружает `ZoneTable[]` по каждой TABLE-зоне на сетке
      (`api.getTables`, как для мест), рендерит ячейку-стол кликабельной
      (клик = +1 место за столом, до `table.available`), номер стола в
      ячейке, легенда показывает суммарный remaining/в корзине
- [x] `RegisterForm`: `tableZones` (плоский `TablePicker`) теперь отфильтрован
      от зон, уже покрашенных на сетке — тот же стол не должен предлагаться
      дважды в двух разных виджетах; прокинуты `cartQuantityByTable`/
      `onTableAdd` в `VenueGridMap`
- [x] Backend `generate-tables` endpoint **не удалён** (bulk-flow) — оставлен
      для обратной совместимости со старыми venue, но в UI больше не
      вызывается нигде
- [x] Typecheck backend + frontend (`tsc --noEmit` + `vite build`) — чисто
- [ ] Ручная проверка в браузере — не выполнена (нет Docker/локальной БД)
- [ ] Подтвердить перед пушем

### Известное ограничение (сознательно, из-за объёма задачи)
Если у зоны уже есть старые «россыпью» столы (создана до этой фичи через
bulk-generate) и админ решит ДОПОЛНИТЕЛЬНО покрасить ту же зону на сетке —
общая вместимость зоны будет суммой старых и новых столов (старые не
подхватываются как «уже размещённые» и не конфликтуют, но и не заменяются).
Это редкий переходный случай: в UI больше нет способа создать
«россыпью»-стол, только через сетку, так что он исчезнет сам по мере
пересоздания старых venue.
