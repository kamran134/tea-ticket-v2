# План: Сетка = реальные зоны/места + зона без мест

## Концепция
«Сетка» (`GridMapEditor`) перестаёт быть черновиком-калькулятором поверх `Venue.gridLayout`
и становится редактором над настоящими `Zone`/`Seat`. Закрашенная ячейка — либо реальное
место (`Seat`, для `type: SEATED`), либо просто часть визуальной области зоны без мест
(`type: GENERAL`, вместимость — отдельное число). Сетка используется и на покупке билета
вместо «Схемы». «Схема» (`ZoneMapEditor`/`VenueMap`) скрывается из UI, код не удаляется.

---

## Этап 1: Схема БД
- [x] Добавить `color String?` в модель `Zone` (для покраски на сетке)
- [x] Миграция `add_zone_color`

## Этап 2: Backend
- [x] `zones.ts`: добавить `color` в `createZoneSchema`/`updateZoneSchema`
- [x] `zones.ts` DELETE `/:id`: перехватывать FK-конфликт (P2003) → 409 с понятным сообщением
- [x] `venues.ts` `PUT /:id/grid-layout`: новая схема `{ rows, cols, cells: string[][] }`
      (без вложенного `zones[]` — ячейка ссылается на реальный `Zone.id`)
- [x] Транзакционная синхронизация мест для `SEATED`-зон при сохранении сетки:
      diff закрашенных ячеек ↔ существующие `Seat` (row/posInSection = координаты ячейки,
      sectionIndex = 0), запрет удаления ячейки/места с активным билетом (409),
      пересчёт `capacity` зоны

## Этап 3: Frontend типы + API
- [x] `types/index.ts`: `color` в `Zone`, `GridLayout` без `zones[]`, убрать `GridZone`
- [x] `api.ts`: обновить `saveGridLayout` под новую схему, вернуть `{ venue, zones }`

## Этап 4: Admin UI — GridMapEditor
- [x] Загружать реальные зоны венью через `api.getZones` (не только из `gridLayout`)
- [x] Форма «+Зона»: номер карты, чекбокс «Без конкретных мест»
      (снят → `SEATED`, отмечен → `GENERAL` + поле «Вместимость»)
- [x] `addZone` → `api.createZone` (реальная зона сразу)
- [x] `removeZone` → `api.deleteZone` (+ обработка 409 «есть билеты»)
- [x] `save()` → новый `api.saveGridLayout`, синхронизация мест на бэке, toast при 409

## Этап 5: Покупка билета
- [x] Новый компонент `VenueGridMap.tsx` — сетка зала, клик по ячейке выбирает зону
- [x] `RegisterForm.tsx`: приоритет gridLayout → legacy VenueMap → карточки
- [ ] Ручная проверка: `SeatPicker` корректно показывает места, сгенерированные из сетки

## Этап 6: Скрыть «Схему»
- [x] `ManagePanel.tsx`: убрать вкладку «Схема» из списка табов (код не трогать)

## Этап 7: Проверка
- [x] Typecheck/build backend и frontend (`tsc --noEmit` + `vite build` — чисто)
- [x] Задеплоено на прод (push в master → GitHub Actions → docker compose up):
      миграция `add_zone_color` применилась автоматически при старте бэкенда,
      колонка `color` подтверждена в БД, tea-ticket.com отдаёт 200, /health — ok
- [ ] Сценарий в браузере: зона с местами через сетку → покупка → занятая ячейка видна
      (не проверено вживую — рекомендуется прогнать на проде вручную)
- [ ] Сценарий в браузере: зона без мест через сетку → покупка по количеству гостей
      (не проверено вживую — рекомендуется прогнать на проде вручную)

---

## Порядок зависимостей
Этап 1 → Этап 2 → Этап 3 → Этап 4 → Этап 5 → Этап 6 → Этап 7

---

# Часть 2: фикс синхронизации мест + убрать номер карты (заглушка оплаты)

## Баг
`PUT /:id/grid-layout` синхронизировал места для *всех* зон типа SEATED в мероприятии,
а не только для тех, что реально участвуют в сетке. Зона, созданная через старую
вкладку «Зоны» (генератор рядов/секций), никогда не красилась на сетке — при любом
сохранении сетки её места считались «удалёнными из сетки» и попадали под защиту от
удаления забронированного места → ошибка 409 на ровном месте.

## Этап 8: Backend
- [x] `venues.ts`: синхронизировать места только для зон, которые сейчас в `cells`
      ИЛИ были в *предыдущем* сохранённом `gridLayout` (а не для всех SEATED-зон венью)
- [x] `zones.ts`: `cardNumber` — необязательное поле в create/update схемах
- [x] `schema.prisma`: `Zone.cardNumber` — `@default("")`, миграция
- [x] `cron.ts`: отключить автоистечение BOOKED через 1 час — оно било в старый флоу
      «не оплатил за час», а без онлайн-оплаты подтверждение полностью ручное

## Этап 9: Frontend — убрать номер карты из форм зон
- [x] `api.ts`: `createZone` — `cardNumber` необязателен
- [x] `ManagePanel.tsx`: убрать поле «Номер карты» из формы создания/редактирования зоны
- [x] `GridMapEditor.tsx`: убрать поле «Номер карты» из формы «+Зона»

## Этап 10: Frontend — заглушка вместо шага оплаты
- [x] `TicketView.tsx`: блок для `status === 'BOOKED'` — вместо номера карты и загрузки
      чека показывать заглушку («оплата картой скоро», без countdown/receipt-upload)
- [x] `ManagePanel.tsx` (вкладка «Билеты»): кнопки Подтвердить/Отклонить — тоже для
      `BOOKED`, не только `PENDING`; фильтр «Новые» (BOOKED) добавлен и стал дефолтным

## Этап 11: Проверка
- [x] Typecheck/build backend и frontend (`tsc --noEmit` + `tsc` + `vite build` — чисто)
- [x] Дождаться подтверждения перед пушем — меняется реальный флоу приёма денег

---

# Часть 3: Афиша на главной + страница мероприятия по слагу

## Концепция
`/` — афиша (карточки с постером, только active + дата в будущем).
`/e/:slug` — страница конкретного мероприятия (текущий RegisterForm, но без
выпадающего списка мероприятий — venue всегда определяется слагом из URL).
Один Vite entry (`index.html`/`main.tsx`), выбор Afisha/RegisterForm по pathname.

## Этап 12: БД
- [x] `Venue.slug String @unique`, `Venue.posterImage String?`
- [x] Миграция: добавить колонки, бэкофилл `slug = id` для существующих строк,
      `NOT NULL` + уникальный индекс

## Этап 13: Backend
- [x] `services/slug.ts` — транслитерация RU→EN + `generateVenueSlug(name, date)`
- [x] `POST /api/venues` — принимает необязательный `slug` (иначе автогенерация),
      409 при коллизии (`P2002`)
- [x] `PATCH /api/venues/:id` — добавить `slug`, `posterImage`
- [x] `GET /api/venues/slug-available?slug=&excludeId=` — проверка занятости (admin)
- [x] `GET /api/venues/by-slug/:slug` — публичный резолвинг для страницы мероприятия
      (только active, иначе 404 — как уже ведёт себя текущий флоу)
- [x] `POST /api/venues/:id/upload-poster` — по аналогии с upload-floor-plan
- [x] `GET /api/venues?upcoming=true` — только active + дата в будущем, сортировка по дате

## Этап 14: Frontend — типы и API
- [x] `types/index.ts`: `Venue.slug`, `Venue.posterImage`
- [x] `api.ts`: `getVenues` с `upcoming`, `getVenueBySlug`, `checkSlugAvailable`, `uploadPoster`,
      `createVenue`/`updateVenue` — `slug`

## Этап 15: Frontend — Афиша + роутинг
- [x] `Afisha.tsx` — карточки (постер/заглушка, название, дата) → ссылка `/e/{slug}`
- [x] `main.tsx` — по pathname рендерит `Afisha` или `RegisterForm(slug)`
- [x] `RegisterForm.tsx` — убрать список мероприятий, резолвить venue по `slug`-пропу
- [x] `nginx.conf` (frontend-контейнер) — `location /e/ { try_files $uri /index.html; }`

## Этап 16: Frontend — админка
- [x] Форма создания мероприятия: поле «Слаг» с live-автогенерацией из
      названия+даты, редактируемое, проверка занятости (debounce)
- [x] На карточке мероприятия: изменить слаг, загрузить постер
- [x] «Скопировать ссылку» → `/e/{slug}` вместо `/?venue={id}`

## Этап 17: Проверка
- [x] Typecheck/build backend и frontend
- [ ] Подтвердить перед пушем — меняется главная страница и формат ссылок
